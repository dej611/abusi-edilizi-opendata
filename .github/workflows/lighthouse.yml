name: Lighthouse CI

on: [pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Deploy for staging
        env:
          ZEIT_TOKEN: ${{ secrets.ZEIT_TOKEN }}
        run: >
          now deploy
          --local-config=./now.json
          --build-env NODE_ENV=production
          --env NODE_ENV=production
          --public
          --debug
          --no-clipboard
          --token ${ZEIT_TOKEN}
          --force
      - name: Alias with PR number
        env:
          ZEIT_TOKEN: ${{ secrets.ZEIT_TOKEN }}
        run: >
          now alias
          --local-config=./now.json
          --token ${ZEIT_TOKEN}
          `cat ${HOME}/deployment-url.txt`
            ${{ github.event.repository.name }}-${{ github.sha }}-pr.now.sh
      - name: Audit URLs using Lighthouse
        uses: treosh/lighthouse-ci-action@v2
        with:
          urls: |
            ${{ github.event.repository.name }}-${{ github.sha }}-pr.now.sh/
            ${{ github.event.repository.name }}-${{ github.sha }}-pr.now.sh/?lang=en
      - name: Save results
        uses: actions/upload-artifact@v1
        with:
          name: lighthouse-results
          path: '.lighthouseci' # This will save the Lighthouse results as .json files
